'use client';   // because the component uses hooks and inline styles

import PrivacyPolicy from "../components/PrivacyPolicy";

export default function PrivacyPolicyPage() {
  return <PrivacyPolicy />;
}